# AssignMe

### To start the app you need to:

## Start the client:

```
npm install
npm run start
```


## Start the server:

```
npm install
node server.mjs
```
### Access to MongoDB is granted from all IPs for one week
